#version 330 core

layout (location = 0) in vec2 aPos;
layout (location = 1) in vec2 aTexCoords;

out vec2 TexCoords;

uniform mat4 transform;
uniform float aspectRatio; 

void main() 
{
	TexCoords = aTexCoords;
	gl_Position = transform * vec4(aPos.x / aspectRatio, aPos.y, 0.0, 1.0);
}
